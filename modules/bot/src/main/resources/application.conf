token = ${TG_BOT_TOKEN}
scrapper-url = ${SCRAPPER_URL}
kafka{
    servers = ${KAFKA_BOOTSTRAP_SERVERS}
    topic = ${KAFKA_LINK_UPDATE_TOPIC}
    consumer-group = ${KAFKA_LINK_UPDATE_CONSUMER_GROUP}
}